version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "ECS_TASK_DEFINITION"
        LoadBalancerInfo:
          ContainerName: "angular-task"
          ContainerPort: 80
